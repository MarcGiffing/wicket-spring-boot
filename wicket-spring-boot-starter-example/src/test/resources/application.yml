spring:
  main:
    allow-bean-definition-overriding: true
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master-test.xml
wicket:
  stuff:
    serializer:
      fast2:
        enabled: false
  core:
    csrf:
      enabled: false
